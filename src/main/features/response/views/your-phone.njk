{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, input, saveAndContinueButton, errorSummary %}

{% set title = t('Money claim') %}
{% set heading = t('Add a contact number (optional)') %}

{% block content %}
  <div class="grid-row">
      <div class="column-two-thirds">

        <p>{{ t('We might need to contact you about this claim. We’ll only call on weekdays between 9am and 5pm.') }}</p>
        <p>{{ t('The claimant and the court can use this to contact you.') }}</p>

        <form method="post">
          {{ csrfProtection(csrf) }}

          {{ input('Add a phone number (optional)', 'number', form, '', labelClass = 'visually-hidden') }}
          {{ saveAndContinueButton() }}
        </form>
      </div>
  </div>
{% endblock %}
