{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, input, saveAndContinueButton, errorSummary %}

{% set title = t('Money claim') %}
{% set heading = t('Add a phone number (optional)') %}

{% block content %}
  <div class="grid-row">
      <div class="column-two-thirds">

        <p>{{ t('We might need to contact you about this claim. We’ll only call on weekdays between 9:30am and 5pm.') }}</p>
        <p>{{ t('We won’t give your number to the claimant.') }}</p>

        <form method="post">
          {{ csrfProtection(csrf) }}

          {{ input('Add a phone number (optional)', 'number', form, '', labelClass = 'visually-hidden') }}
          {{ saveAndContinueButton() }}
        </form>
      </div>
  </div>
{% endblock %}
