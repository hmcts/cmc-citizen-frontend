{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, submitButton %}

{% set heading = 'How free mediation works' %}

{% block content %}
  <div class="grid-row">
    <div class="column-two-thirds">
      <h2 class="heading-medium">{{ t('A mediator phones you') }}</h2>
      <p>{{ t('We’ll contact you to arrange a phone call with a mediator from the Small Claims Mediation Service.') }}</p>
      <p>{{ t('The call won’t take longer than an hour.') }}</p>

      <h2 class="heading-medium">{{ t('You discuss the claim') }}</h2>
      <p>{{ t('The mediator will:') }}</p>
      <ul class="list list-bullet">
        <li>{{ t('listen to each side separately - you won’t talk directly to the other side') }}</li>
        <li>{{ t('ask you to consider what the other side says') }}</li>
        <li>{{ t('help you reach an agreement, if possible') }}</li>
      </ul>

      <h2 class="heading-medium">{{ t('After the phone call') }}</h2>
      <p>{{ t('If mediation is successful you’ll make a verbal agreement over the phone. We’ll email the agreement to you. You won’t have to sign anything but the agreement is binding.') }}</p>
      <p>{{ t('If either party breaks the terms, the other party can apply to the court to enforce them.') }}</p>
      <p>{{ t('If mediation fails and a hearing is needed, any mediation discussions can’t be mentioned in court.') }}</p>

      <form method="post" novalidate>
        {{ csrfProtection(csrf) }}

        <div class="form-group">
          {{ submitButton() }}
        </div>
      </form>
    </div>
  </div>
{% endblock %}

