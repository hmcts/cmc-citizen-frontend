{% extends "layout.njk" %}
{% from "form.njk" import nameInput, saveAndContinueButton, csrfProtection, errorSummary %}

{% set heading = 'Their details' %}

{% block content %}
  <div class="grid-row">
    <div class="column-two-thirds">
      <form novalidate method="post">
        {{ csrfProtection(csrf) }}

        {{ nameInput(label = 'Their full name', form) }}

        {{ saveAndContinueButton() }}
      </form>
    </div>
  </div>
{% endblock %}
