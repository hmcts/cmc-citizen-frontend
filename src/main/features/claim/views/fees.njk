{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, submitButton %}
{% from "feesTable.njk" import feesTable %}

{% set title = t('Fees | Money claim') %}
{% set heading = t('Fees you’ll pay') %}

{% block content %}

  <div class="grid-row">
    <div class="column-two-thirds">

      <form novalidate method="post">
        {{ csrfProtection(csrf) }}
        <p>
          {{ t('If you’re on a low income or get certain benefits you could get financial help with court fees.') }}
        </p>

        <table class="form-group">
          <thead>
          <tr>
            <th scope="col"></th>
            <th class="numeric" scope="col"></th>
          </tr>
          </thead>
          <tbody>

          <tr>
            <td>{{ t('Claim fee') }}</td>
            <td class="numeric">£{{ issueFee }}</td>
          </tr>

          <tr>
            <td>{{ t('Hearing fee (if your claim goes to a hearing)') }}</td>
            <td class="numeric">£{{ hearingFee }}</td>
          </tr>

          </tbody>
        </table>

        {{ feesTable('How these fees are calculated', rows , showHearingFee = true) }}

        <div class="form-group">
          {{ submitButton('Continue') }}
        </div>
      </form>
    </div>
  </div>

{% endblock %}
