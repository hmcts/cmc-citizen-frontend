{% extends "layout.njk" %}

{% set title = t('Money claim | Make a legal agreement') %}

{% block content %}
  <div class="grid-row">
    <div class="column-two-thirds">

      <h1 class="heading-large">{{ t('You can now make a legal agreement') }}</h1>

      <p>
        {{ t('The agreement will be based on the offer from {{ name }} you`ve have just accepted. It has not been reviewed by the court staff or judge.', {
               name: claim.claimData.defendant.name
             })
        }}
      </p>

      <p>
        {{ t('You can avoid getting a County Court Judgment (CCJ) if you settle out of court. ') }}
        {{ t('There will be no public record of the claim.') }}
      </p>

      <p>
        {{
          t('You both need to sign the agreement for it to become a legal contract.', {
            name: claim.claimData.defendant.name
          })
        }}
      <p>

      <a href="{{ OfferPaths.declarationPage.evaluateUri({ externalId: claim.externalId }) }}" role="button" class="button">
        {{ t('Make an agreement') }}
      </a>
    </div>
  </div>
{% endblock %}
