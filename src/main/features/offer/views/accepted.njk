{% extends "layout.njk" %}
{% from "support.njk" import supportWidget %}

{% set title = t('Money claim | Offer accepted - confirmation page') %}

{% block content %}
  <div class="grid-row">
    <div class="column-two-thirds">
      <div class="govuk-box-highlight">
        <h1 class="heading-large">{{ t('You’ve signed the agreement') }}</h1>
      </div>

      <h2 class="heading-medium">{{ t('What happens next') }}</h2>
      <p>
        {{
          t('{{ name }} will also be asked to sign. Once signed this agreement replaces the court claim.', {
            name: claim.claimData.defendant.name
          })
        }}
      </p>

      <br>
      <a href="{{ dashboardLink }}" class="button">Finished</a>

      {{ supportWidget() }}

      {% include "includes/service_survey_link.njk" %}
    </div>
  </div>
{% endblock %}
