{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, submitButton %}
{% from "support.njk" import supportWidget %}
{% set title = t('Money claim') %}
{% set heading = t('Make an offer to settle out of court') %}
{% block content %}
  <div class="grid-row">
    <div class="column-two-thirds">
        <p>{{ t('You can avoid getting a County Court Judgment (CCJ) if you settle out of court. There will be no public record of the claim.') }}</p>
        <p>
        {{ t('If {{ claimantName }} accepts your offer you’ll both sign a legal agreement.', { claimantName: claim.claimData.claimant.name }) }}
      </p>
      <p>
        {{ t('If they reject your offer the details can’t be mentioned in court.') }}
      </p>

      <p>{{ t('If {{ claimant }} accepts your offer you’ll both sign a legal agreement. This agreement will replace the claim.', { claimant: claim.claimData.claimant.name  }) | safe}}</p>

      <p>{{ t('If {{ claimant }} rejects your offer it can’t be mentioned in court.', { claimant: claim.claimData.claimant.name  }) | safe}}</p>

      <p>{{ t('<a href="{{ link }}" class="button">Continue</a>',
      { link: OfferPaths.offerPage.evaluateUri({ externalId: claim.externalId }) }) | safe }}</p>
    </div>
  </div>
{% endblock %}
