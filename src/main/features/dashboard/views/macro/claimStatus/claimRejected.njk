{% from "externalLink.njk" import externalLink %}
{% from "internalLink.njk" import internalLink %}

{% macro claimRejectedAlreadyPaidForClaimantDashboard(claim) %}
  {{ t('{{ defendantName }} believes that they’ve paid the claim in full.',
    { defendantName: claim.claimData.defendant.name }) }}
{% endmacro %}

{% macro claimRejectedAlreadyPaidForClaimantClaimDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('The defendant’s response') }}</h2>
    <p>{{ t('{{ defendantName }} says they paid you {{ amount }} on {{ paymentDate }}.',
        { defendantName: claim.claimData.defendant.name, amount: claim.response.paymentDeclaration.paidAmount | numeral, paymentDate: claim.response.paymentDeclaration.paidDate | date }) }}</p>
    <p>{{ t('You can accept or reject this response.') }}</p>
    <p>{{ _respond(claim, 'View and respond') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedAlreadyPaidForDefendantDashboard(claim) %}
  {{ t('We’ve emailed {{ claimantName }} telling them when and how you said you paid the claim.',
    { claimantName: claim.claimData.claimant.name }) }}
{% endmacro %}

{% macro claimRejectedAlreadyPaidForDefendantClaimDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('Your response to the claim') }}</h2>
    <p>{{ t('We’ve emailed {{ claimantName }} telling them when and how you said you paid the claim.',
        { claimantName: claim.claimData.claimant.name }) }}</p>
    <p>{{ t('We’ll contact you to let you know how they respond. They can confirm you’ve paid and the claim is ' +
        'settled, or they can proceed with it.') }}</p>
    <p>{{ _downloadResponseLink(claim, 'Download your response') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedFreeMediationNOForClaimantDashboard(claim) %}
  {{ t('{{ defendantName }} has rejected your claim.',
    { defendantName: claim.claimData.defendant.name }) }}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationNOForClaimantDashboard(claim) %}
  {{ t('You’ve reject the defendant’s response.') }}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationNOForDefendantDashboard(claim) %}
  {{ t('Wait for the court to review the case') }}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForDefendantDashboard(claim) %}
  {{ t('We’ll contact you with a mediation appointment') }}
{% endmacro %}

{% macro claimantAcceptedDefendantDefenceDefendantDashboard(claim) %}
  {{ t('The claim has been settled') }}
{% endmacro %}

{% macro claimantAcceptedDefendantDefenceClaimantDashboard(claim) %}
  {{ t('You accepted the defendants defence') }}
{% endmacro %}

{% macro claimantAcceptedDefendantDefenceDefendantClaimDetails(claim) %}
  {{ t('You accepted the defendants defence') }}
{% endmacro %}

{% macro claimantAcceptedDefendantDefenceClaimantClaimDetails(claim) %}
  {{ t('You accepted the defendants defence') }}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationNOForDefendantDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('Wait for the court to review the case') }}</h2>
    <p>{{ t('{{ claimantName }} has rejected your defence.',
        { claimantName: claim.claimData.claimant.name }) }}</p>
    <p>{{ t('The court will review the case. We’ll email you if we set a hearing date to tell you how to prepare.') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForDefendantDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('We’ll contact you with a mediation appointment') }}</h2>
    <p>{{ t('{{ claimantName }} has rejected your defence.',
        { claimantName: claim.claimData.claimant.name }) }}</p>
    <p>{{ t('You’ve both agreed to try mediation. We’ll contact you to arrange a call with the mediator.') }}</p>
    <p>{{ internalLink(t('Find out how mediation works'), ClaimantResponsePaths.taskListPage.evaluateUri({ externalId: claim.externalId })) }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationNOForClaimantDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('Wait for the court to review the case') }}</h2>
    <p>{{ t('You’ve rejected defendant’s response and said you want to take the case to court.') }}</p>
    <p>{{ t('The court will review the case. We’ll email you if we set a hearing date to tell you how to prepare.') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedFreeMediationNOForDefendantDashboard() %}
  {{ t('You’ve rejected the claim. You need to tell us more about the claim.') }}
{% endmacro %}

{% macro claimRejectedFreeMediationNOForDefendantClaimDetails(claim) %}
  {% if featureToggles.mediation %}
    <div class="status-block">
      <h2 class="heading-small">{{ t('Wait for the claimant to respond') }}</h2>
      <p>{{ t('You’ve rejected the claim.') }}</p>
      <p>{{ t('You said you don’t want to use mediation to solve it. You might have to go to a hearing.') }}</p>
      <p>{{ t('We’ll contact you when the claimant responds.') }}</p>
    </div>
  {% else %}
    <div class="status-block">
      <h2 class="heading-small">{{ t('Your response to the claim') }}</h2>
      <p>{{ t('You’ve rejected the claim and said you don’t want to use mediation to solve it. You’ll have to go to a hearing.') }}</p>
      {{ _linkToDirectionsQuestionnairePage(claim.externalId) }}
      <p>{{ t('Your defence will be cancelled if you don’t complete and return the form before 4pm on {{ deadline }}.',
          { deadline: claim.directionsQuestionnaireDeadline | date }) }}</p>
      <p>{{ _downloadResponseLink(claim, 'Download your response') }}</p>
    </div>
  {% endif %}
{% endmacro %}

{% macro claimRejectedFreeMediationYESForDefendantDashboard() %}
  {{ t('You’ve rejected the claim and suggested mediation. We’ll ask the claimant if they agree to take part in mediation.') }}
{% endmacro %}

{% macro claimRejectedFreeMediationYESForDefendantClaimDetails(claim) %}
  {% if featureToggles.mediation %}
    <div class="status-block">
      <h2 class="heading-small">{{ t('Wait for the claimant to respond') }}</h2>
      <p>{{ t('You’ve rejected the claim.') }}</p>
      <p>{{ t('You said you don’t want to use mediation to solve it. You might have to go to a hearing.') }}</p>
      <p>{{ t('We’ll contact you when the claimant responds.') }}</p>
    </div>
  {% else %}
    <div class="status-block">
      <h2 class="heading-small">{{ t('Your response to the claim') }}</h2>
      <p>{{ t('You have rejected the claim. You’ve suggested mediation.') }}</p>
      <p>{{ t('We’ll ask {{ claimantName }} if they agree to take part in mediation.',
          { claimantName: claim.claimData.claimant.name }) }}</p>
      <p>{{ t('If they say yes, we’ll arrange a date for mediation. If they say no we’ll send you a directions questionnaire - '
          + 'this is a form you complete to tell us more about the claim.') }}</p>
      <p>{{ _downloadResponseLink(claim, 'Download your response') }}</p>
    </div>
  {% endif %}
{% endmacro %}

{% macro claimRejectedFreeMediationYESForClaimantDashboard(claim) %}
  {{ t('{{ defendantName }} has rejected the claim. They’ve suggested a mediation session to help resolve this dispute.',
    { defendantName: claim.claimData.defendant.name }) }}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForClaimantDashboard(claim) %}
  {{ t('You’ve rejected the defendant’s response.') }}

  {% if claim.claimantResponse.freeMediation === FreeMediationOption.YES %}
    <p>{{ t('You’ve both agreed to try mediation.') }}
  {% endif %}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForClaimantDashboard(claim) %}
  {{ t('You’ve rejected the defendant’s response.') }}

  {% if claim.claimantResponse.freeMediation === FreeMediationOption.YES %}
    <p>{{ t('You’ve both agreed to try mediation.') }}
  {% endif %}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForClaimantDashboard(claim) %}
    {{ t('You’ve rejected the defendant’s response.') }}

    {% if claim.claimantResponse.freeMediation === FreeMediationOption.YES %}
      <p>{{ t('You’ve both agreed to try mediation.') }}
    {% endif %}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForClaimantDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('You’ve rejected the defendant’s response.') }}</h2>

    {% if claim.claimantResponse.freeMediation === FreeMediationOption.YES %}
      <p>{{ t('You’ve both agreed to try mediation. The Small Claims Mediation Service will contact you to arrange a call with the mediator.') }}</p>
      {{ externalLink(t('Find out how mediation works'), 'https://www.gov.uk/government/publications/small-claims-mediation-service-ex730') }}
    {% endif %}

    <p>{{ _downloadResponseLink(claim, 'Download their response') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForClaimantDashboard(claim) %}
  {{ t('You’ve rejected the defendant’s response.') }}

  {% if claim.claimantResponse.freeMediation === FreeMediationOption.YES %}
    <p>{{ t('You’ve both agreed to try mediation.') }}</p>
  {% endif %}
{% endmacro %}

{% macro claimRejectedDefendantDefenceFreeMediationYESForClaimantDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('You’ve rejected the defendant’s response.') }}</h2>
    <p>{{ t('You’ve both agreed to try mediation. The Small Claims Mediation Service will contact you to arrange a call with the mediator.') }}</p>
    {{ externalLink('Find out how mediation works', 'https://www.gov.uk/government/publications/small-claims-mediation-service-ex730') }}
    <p>{{ _downloadResponseLink(claim, 'Download your response') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedWithDQsForClaimantClaimDetails(claim, mediationDeadline) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('Decide whether to proceed') }}</h2>
    <p>{{ t('{{ defendantName }} has rejected your claim.', { defendantName: claim.claimData.defendant.name }) }}</p>
    <p>{{ t('You need to decide whether to proceed with the claim. You need to respond before 4pm on {{ respondToMediationDeadline }}. Your claim won’t continue if you don’t respond by then.',
        { respondToMediationDeadline: mediationDeadline | date }) }}</p>
    <p>{{ _respond(claim, 'View and respond') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedWithDQsForDefendantClaimDetails(claim, mediationDeadline) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('Wait for the claimant to respond') }}</h2>
    {% if claim.response.freeMediation === YesNoOption.Yes %}
      <p>{{ t('You have rejected the claim. You’ve suggested mediation.') }}</p>
      <p>{{ t('We’ll ask {{ claimantName }} if they agree to take part in mediation.',{ claimantName: claim.claimData.claimant.name }) }}</p>
      <p>{{ t('If they agree, we’ll contact you with details of your appointment. If not, you might have to go to a hearing.') }}</p>
    {% endif %}

    <p>{{ t('{{ defendantName }} has rejected your claim.', { defendantName: claim.claimData.defendant.name }) }}</p>
    <p>{{ t('You need to respond before 4pm on {{ respondToMediationDeadline }}. Your claim won’t continue if you don’t respond by then.',
        { respondToMediationDeadline: mediationDeadline | date }) }}</p>
    <p>{{ _respond(claim, 'View and respond') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedWithDQsForClaimantDashboard(claim) %}
  {{ t('Decide whether to proceed') }}
{% endmacro %}

{% macro claimRejectedWithDQsForDefendantDashboard(claim) %}
  {{ t('Wait for the claimant to respond') }}
{% endmacro %}

{% macro claimRejectedFreeMediationYESForDefendantDashboard() %}
  {{ t('You’ve rejected the claim and suggested mediation. We’ll ask the claimant if they agree to take part in mediation.') }}
{% endmacro %}

{% macro claimRejectedFreeMediationYESForDefendantClaimDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('Your response to the claim') }}</h2>
    <p>{{ t('You have rejected the claim. You’ve suggested mediation.') }}</p>
    <p>{{ t('We’ll ask {{ claimantName }} if they agree to take part in mediation.',
        { claimantName: claim.claimData.claimant.name }) }}</p>
    <p>{{ t('If they say yes, we’ll arrange a date for mediation. If they say no we’ll send you a directions questionnaire - '
        + 'this is a form you complete to tell us more about the claim.') }}</p>
    <p>{{ _downloadResponseLink(claim, 'Download your response') }}</p>
  </div>
{% endmacro %}

{% macro _downloadResponseLink(claim, text) %}
  {{ internalLink(t(text), ResponsePaths.receiptReceiver.evaluateUri({ externalId: claim.externalId })) }}
{% endmacro %}

{% macro _linkToDirectionsQuestionnairePage(externalId) %}
  <p>{{ t('You need to {{ completeDQ }} form to tell us more about the claim.',
        { completeDQ: internalLink(
          'complete a directions questionnaire',
          DashboardPaths.directionsQuestionnairePage.evaluateUri({ externalId: externalId }))
        }
      ) | safe
  }}</p>
{% endmacro %}

{% macro _respond(claim, text) %}
  <p>{{ internalLink(t(text), ClaimantResponsePaths.taskListPage.evaluateUri({ externalId: claim.externalId }), 'button button-primary') }}</p>
{% endmacro %}

{% macro claimRejectedForClaimantClaimDetails(claim, mediationDeadline) %}
  {% if featureToggles.mediation %}
    <div class="status-block">
      <h2 class="heading-small">{{ t('Decide whether to proceed') }}</h2>
      <p>{{ t('{{ defendantName }} has rejected your claim.', { defendantName: claim.claimData.defendant.name }) }}</p>
      <p>{{ t('You need to decide whether to proceed with the claim. You need to respond before 4pm on {{ respondToMediationDeadline }}. Your claim won’t continue if you don’t respond by then.',
          { respondToMediationDeadline: mediationDeadline | date }) }}</p>
      <p>{{ internalLink(t('View and respond'), ClaimantResponsePaths.taskListPage.evaluateUri({ externalId: claim.externalId }), 'button button-primary') }}</p>
    </div>
  {% elif claim.response.freeMediation === YesNoOption.YES.option %}
    <div class="status-block">
      <h2 class="heading-small">{{ t('The defendant’s response') }}</h2>
      <p>{{ t('{{ defendantName }} has rejected the claim. They’ve suggested mediation to help resolve this dispute.',
          { defendantName: claim.claimData.defendant.name }) }}</p>
      <p>{{ t(externalLink('Find out how mediation works', 'https://formfinder.hmctsformfinder.justice.gov.uk/ex730-eng.pdf') + ' (PDF, 399KB)') | safe }}</p>
      {% if mediationDeadline %}
        <p>{{ t('You need to email {{ emailAddress }} before 4pm on {{ respondToMediationDeadline }} to tell us if you agree to mediation or not.',
            { emailAddress: supportEmailAddress, respondToMediationDeadline: mediationDeadline | date }) }}</p>
        <p>{{ t('If you don’t send an email before the deadline, the claim will proceed without mediation.') }}</p>
      {% endif %}
      <p>{{ _downloadResponseLink(claim, 'Download their response') }}</p>
    </div>
  {% else %}
    <div class="status-block">
      <h2 class="heading-small">{{ t('The defendant has rejected your claim') }}</h2>
      <p>{{ t('They said they dispute your claim.') }}</p>
      {{ _linkToDirectionsQuestionnairePage(claim.externalId) }}
      <p>{{ t('Your claim won’t proceed if you don’t complete and return the form before 4pm on {{ deadline }}.',
          { deadline: claim.directionsQuestionnaireDeadline | date }) }}</p>
      <p>{{ _downloadResponseLink(claim, 'Download their response') }}</p>
    </div>
  {% endif %}
{% endmacro %}

{% macro claimRejectedFreeMediationNOWithoutDQsForClaimantClaimDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('The defendant has rejected your claim') }}</h2>
    <p>{{ t('They said they dispute your claim.') }}</p>
    {{ _linkToDirectionsQuestionnairePage(claim.externalId) }}
    <p>{{ t('Your claim won’t proceed if you don’t complete and return the form before 4pm on {{ deadline }}.',
        { deadline: claim.directionsQuestionnaireDeadline | date }) }}</p>
    <p>{{ _downloadResponseLink(claim, 'Download their response') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedFreeMediationNOWithoutDQsForDefendantClaimDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-small">{{ t('Your response to the claim') }}</h2>
    <p>{{ t('You’ve rejected the claim and said you don’t want to use mediation to solve it. You’ll have to go to a hearing.') }}</p>
    {{ _linkToDirectionsQuestionnairePage(claim.externalId) }}
    <p>{{ t('Your defence will be cancelled if you don’t complete and return the form before 4pm on {{ deadline }}.',
        { deadline: claim.directionsQuestionnaireDeadline | date }) }}</p>
    <p>{{ _downloadResponseLink(claim, 'Download your response') }}</p>
  </div>
{% endmacro %}

{% macro claimRejectedFreeMediationNOWithoutDQsForClaimantDashboard() %}
  {{ t('You’ve rejected their defence. You need to tell us more about the claim.') }}
{% endmacro %}

{% macro claimRejectedFreeMediationNOWithoutDQsForDefendantDashboard() %}
  {{ t('You’ve rejected the claim and suggested mediation. We’ll ask the claimant if they agree to take part in mediation.') }}
{% endmacro %}

{% macro reviewRequester(claim) %}
  <h2 class="heading-medium">{{ t('You’ve asked the court to review the order') }} </h2>
  {% if(claim.reviewOrder.requestedBy === 'CLAIMANT') %}
    <p>{{ t('We’ll tell {{ defendantName }}. They can comment until 25 August 2019.',{defendantName: claim.claimData.defendants[0].name}) }}</p>
  {% else %}
    <p>{{ t('We’ll tell {{ claimantName }}. They can comment until 25 August 2019.',{claimantName: claim.claimData.claimants[0].name}) }}</p>
  {% endif %}
  <p>{{ t('A judge will then review the order and any comments. We’ll contact you by post to tell you what to do next.') }}</p>
  <p>{{ t('Continue doing everything the current order tells you to do unless we tell you a judge has made a new one.') }}</p>
  <div class="bottom-border"></div>
{% endmacro %}

{% macro reviewReceiver(claim) %}
  <h2 class="heading-small">{{ t('Read {{ defendantName }}’ request',{defendantName: claim.claimData.defendants[0].name}) }}</h2>
  <p>{{ t('{{ defendantName }} has asked the court to change the order.',{defendantName: claim.claimData.defendants[0].name}) }}</p>

  <p>{{ t('A judge will review their request and any comments you’ve sent. We’ll contact you by post if the judge makes a new order.') }}</p>

  <p>{{ t('Continue doing everything the current order tells you to do unless we tell you a judge has made a new one.') }}</p>

  {{ internalLink(t('Download {{ defendantName }}’ request to see what they asked ',{defendantName: claim.claimData.defendants[0].name}), ResponsePaths.receiptReceiver.evaluateUri({ externalId: claim.externalId })) }}
  {{ t('(PDF)') }} to find out the details you need to send.


  <h2 class="heading-small">{{ t('Comment on {{ defendantName }}’ request',{defendantName: claim.claimData.defendants[0].name}) }}</h2>

  <p>{{ t('If you want to comment on the request, email your comments to the court and to {{ defendantName }} before 4pm on 25 August 2019.', {defendantName: claim.claimData.defendants[0].name}) }}</p>
  <p>{{ t('Include the claim number ({{claimNumber}}) in the emails.',{claimNumber: claim.claimNumber}) }}</p>
  <p>{{ t('The court’s email address is:') }} <a href="#"> moneyclaims@justice.gov.uk </a></p>
  <p>{{ t('{{ defendantName }}’ email address is:',{defendantName: claim.claimData.defendants[0].name}) }} <a href="#"> mary.richards@gmail.com </a></p>

  <div class="bottom-border"></div>
{% endmacro %}

{% macro directionsOrderIssuedForDefendant(claim) %}
  <h2 class="heading-small">{{ t('Send us more details before the hearing') }}</h2>
  <p>{{ t('The court has ordered you to send more details.') }}</p>
  <p>
  {{ internalLink(t('Download the court’s order'), DashboardPaths.directionsOrderDocument.evaluateUri({ externalId: claim.externalId })) }}
  {{ t('(PDF)') }} to find out the details you need to send.
  </p>
  <p>{{ t('Make sure you include the claim number ({{claimNumber}}) in the letter.',{claimNumber: claim.claimNumber}) }}</p>
  <h2 class="heading-small">{{ t('Deadline for posting the details') }}</h2>
  <p> {{ t('You must make sure the court and the claimant receive a letter with the details before 4pm on {{ postDocumentsLastDay }}',{postDocumentsLastDay:claim.directionOrder.postDocumentsLastDay | date}) }}

  <h2 class="heading-small">{{ t('Where to post the details') }}
  <p>{{ t('The court’s address is:') }}</p>
  <p>
    Civil Money Claims<br>
    {{ claim.directionOrder.hearingCourtAddress.line1 }}<br>
    {{ claim.directionOrder.hearingCourtAddress.line2 }}<br>
    {{ claim.directionOrder.hearingCourtAddress.city }}<br>
    {{ claim.directionOrder.hearingCourtAddress.postcode }}
  </p>
  <p>
    {{ t('The claimant’s address is:') }}<br>
  {{ claim.claimData.claimants[0].address.line1 }}<br>
  {{ claim.claimData.claimants[0].address.line2 }}<br>
  {{ claim.claimData.claimants[0].address.line3 }}<br>
  {{ claim.claimData.claimants[0].address.city }}<br>
  {{ claim.claimData.claimants[0].address.postcode }}</p>

  <h2 class="heading-small">{{ t('If you don’t send us more details') }}</h2>
  <p>{{ t('The court can cancel your defence and the claimant can request a County Court Judgment (CCJ) against you.') }}</p>

  {% if(claim.directionOrder.isReviewOrderEligible and claim.reviewOrder === undefined) %}
    <h2 class="heading-small">{{ t('If you have a problem with the order') }}</h2>
    <p>
      You can {{ internalLink(t('ask the court to review it.'), DashboardPaths.directionsOrderDocument.evaluateUri({ externalId: claim.externalId })) }}
      {{ t('You need to do this before 4pm on {{ reviewLastDay }}',{reviewLastDay : claim.directionOrder.reviewLastDay | date}) }}
    </p>
    <p>
      {{ t('A judge will consider your request. You should give details of how you want the order changed and the reasons for your request.') }}
    </p>
  {% endif %}

{% endmacro %}

{% macro directionsOrderIssuedForClaimant(claim) %}
  <h2 class="heading-small">{{ t('Send us more details before the hearing') }}</h2>
  <p>{{ t('The court has ordered you to send more details.') }}</p>
  <p>
    {{ internalLink(t('Download the order'), DashboardPaths.directionsOrderDocument.evaluateUri({ externalId: claim.externalId })) }}
    {{ t('(PDF)') }} to find out the details you need to send.
  </p>
  <p>{{ t('Make sure you include the claim number ({{claimNumber}}) in the letter.',{claimNumber: claim.claimNumber}) }}</p>

  <h2 class="heading-small">{{ t('Deadline for posting the details') }}</h2>
  <p> {{ t('You must make sure the court and the defendant receive a letter with the details before 4pm on {{ postDocumentsLastDay }}',{postDocumentsLastDay:claim.directionOrder.postDocumentsLastDay | date}) }}

    <h2 class="heading-small">{{ t('Where to post the details') }}
  <p>{{ t('The court’s address is:') }}</p>
  <p>
    Civil Money Claims<br>
    {{ claim.directionOrder.hearingCourtAddress.line1 }}<br>
    {{ claim.directionOrder.hearingCourtAddress.line2 }}<br>
    {{ claim.directionOrder.hearingCourtAddress.city }}<br>
    {{ claim.directionOrder.hearingCourtAddress.postcode }}
  </p>
  <p>
    {{ t('The defendant’s address is:') }}
  </p>
  <p>{{ claim.claimData.defendants[0].address.line1 }}<br>
    {{ claim.claimData.defendants[0].address.line2 }}<br>
    {{ claim.claimData.defendants[0].address.line3 }}<br>
    {{ claim.claimData.defendants[0].address.city }}<br>
    {{ claim.claimData.defendants[0].address.postcode }}</p>

  <h2 class="heading-small">{{ t('If you don’t send us more details') }}</h2>
  <p>{{ t('The court can cancel your claim after 4pm on {{ postDocumentsLastDay }}',{postDocumentsLastDay:claim.directionOrder.postDocumentsLastDay | date}) }}</p>
  {% if(claim.directionOrder.isReviewOrderEligible and claim.reviewOrder === undefined) %}
    <h2 class="heading-small">{{ t('If you have a problem with the order') }}</h2>

    <p>
      You can {{ internalLink(t('ask the court to review it.'), ResponsePaths.receiptReceiver.evaluateUri({ externalId: claim.externalId })) }}
      {{ t('You need to do this before 4pm on {{ reviewLastDay }}',{reviewLastDay : claim.directionOrder.reviewLastDay | date}) }}
    </p>
    <p>
      {{ t('A judge will consider your request. You should give details of how you want the order changed and the reasons for your request.') }}
    </p>
  {% endif %}
{% endmacro %}



