{% from "externalLink.njk" import externalLink %}

{% macro ccjRequestedForClaimantDashboard(claim) %}
  {{ t('Wait for the judgment to be confirmed') }}
{% endmacro %}

{% macro ccjRequestedForClaimantClaimDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-medium">{{ t('Wait for the judgment to be confirmed') }}</h2>
    <p>{{ t('We’ll contact you within 10 working days to tell you whether the judgment has been entered.') }}</p>
    <p>{{ t('{{ defendantName }} can no longer respond to your claim using this service - they may have responded by post.',
        { defendantName: claim.claimData.defendant.name }) }}</p>
    <p>{{ t('It’s possible we received a postal response before the deadline but hadn’t processed it when you requested judgment.') }}</p>
    <p>{{ t('If we discover this has happened, we’ll reject your request.') }}</p>
  </div>
{% endmacro %}

{% macro ccjRequestedForDefendantDashboard(claim) %}
  {{ t('The claimant has requested a County Court Judgment (CCJ) against you on {{ ccjRequestedAt }}.',
    { ccjRequestedAt: claim.countyCourtJudgmentRequestedAt | date }) }}
{% endmacro %}

{% macro ccjRequestedForDefendantClaimDetails(claim) %}
  <div class="status-block">
    <h2 class="heading-medium">{{ t('We’ll contact you') }}</h2>
    <p>{{ t('{{ claimantName }} has requested a County Court Judgment (CCJ) against you because the deadline for your response has passed.',
        { claimantName : claim.claimData.claimant.name }) }}</p>
    <p>{{ t('If you’ve responded by post before the deadline, we may still be processing your response. If we receive your postal response, we’ll reject the request for a CCJ.') }}</p>
    <p>{{ t('Otherwise we’ll post a copy of the CCJ to you and to {{ claimantName }} and explain what to do next.', { claimantName : claim.claimData.claimant.name }) }}</p>
  </div>
{% endmacro %}
