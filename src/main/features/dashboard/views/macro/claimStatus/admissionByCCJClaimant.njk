{% from "internalLink.njk" import internalLink %}
{% from "form.njk" import linkButton %}
{% from "./paidInFullClaimant.njk" import paidInFullClaimantButtonClaimDetails %}

{% macro admissionByCCJClaimantDashboard(claim) %}
  {{ t('A County Court Judgment has been issued.') }}
{% endmacro %}

{% macro admissionByCCJClaimantClaimDetails(claim) %}
  <h2 class="heading-small">{{ t('A County Court Judgment has been issued') }}</h2>
  <p>{{ t('We’ll post a copy of the judgment to you and to {{ defendantName }}',
      { defendantName: claim.claimData.defendant.name }) }}</p>
  <p>{{ internalLink('View the repayment plan', CCJPaths.repaymentPlanSummaryPage.evaluateUri({ externalId: claim.externalId })) }}</p>
  <p> {{ t('When you’ve been paid in full, you need to let us know.') }} </p>
  {{ paidInFullClaimantButtonClaimDetails(claim) }}
  <div class="status-block">
    <h2 class="heading-small">{{ t('A County Court Judgment has been issued') }}</h2>
    <p>{{ t('We’ll post a copy of the judgment to you and to {{ defendantName }}',
        { defendantName: claim.claimData.defendant.name }) }}</p>
    <p>{{ internalLink('View the repayment plan', CCJPaths.repaymentPlanSummaryPage.evaluateUri({ externalId: claim.externalId, madeBy: MadeBy.CLAIMANT.value })) }}</p>
    <p> {{ t('When you’ve been paid in full, you need to let us know.') }} </p>
    {{ paidInFullClaimantButtonClaimDetails(claim,'Tell us you’ve been paid') }}
  </div>
{% endmacro %}
