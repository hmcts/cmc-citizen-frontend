{% extends "layout.njk" %}

{% set title = 'Money claim' %}

{% from "includes/macros/form.njk" import saveAndContinueButton, csrfProtection, addressInput, errorSummary %}

{% block content %}
  <div class="grid-row">
    <div class="column-two-thirds">
      <form method="post" novalidate>
        {{ csrfProtection(csrf) }}

        <h1 class="heading-large">
          <span class="heading-secondary">{{ t('County Court Judgment') }}</span>
          {{ t('Confirm their details') }}
        </h1>

        <p>{{ t('Check their address is right. You can correct or update address.') }}</p>
        <div>
          <h2 class="heading-medium">{{ t('Full name') }}</h2>
          <p>{{ claim.claimData.defendant.name }}</p>

          <h2 class="heading-medium">{{ t('Their address') }}</h2>
          {{ addressInput(form, 'address') }}


          <h2 class="heading-medium">{{ t('Email address') }}</h2>
          <p>{{ claim.claimData.defendant.email }}</p>

          {{ saveAndContinueButton() }}
        </div>
      </form>
    </div>
  </div>
{% endblock %}
