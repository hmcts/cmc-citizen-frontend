{% macro internalLink(text, href, class='', id='', dataEventLabel='') %}
  <a href="{{ href }}" class="{{ class }}"{{ ' id="' + id + '"' if id else '' }}{{ ' dataEventLabel="' + dataEventLabel + '"' if dataEventLabel else '' }}>{{ text }}</a>
{% endmacro %}
