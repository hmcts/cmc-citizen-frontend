{% extends "layout.njk" %}
{% from "form.njk" import csrfProtection, textInput, submitButton, errorSummary %}

{% set title = 'Enter your claim number' %}
{% set heading = 'Enter your claim number' %}

{% block content %}
  <div class="grid-row">
    <div class="column-two-thirds">

      <form method="post">
        {{ csrfProtection(csrf) }}

        {{ textInput('Claim number', 'reference', form) }}
        {{ submitButton('Continue') }}

        <p>
          <a href="{{ AppPaths.noClaimNumberPage.uri }}">{{ t('Don’t have a claim number?') }}</a>
        </p>
      </form>
    </div>
  </div>

{% endblock %}
