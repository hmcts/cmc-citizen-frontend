import { RoutablePath } from 'main/common/router/routablePath'

export class Paths {
  static readonly paidAmountPage = new RoutablePath('/ccj/paid-amount')
  static readonly paidAmountSummaryPage = new RoutablePath('/ccj/paid-amount-summary')
}
