#!groovy

@Library("Infrastructure@feature/Enable-deployments-for-cnp-branch")

def product = "cmc"
def component = "citizen-frontend"

withPipeline("nodejs", product, component) {
  after('checkout') {
    echo 'citizen-frontend checked out'
  }

  after('build') {
    sh 'yarn setup'
  }

  onSuccess {
    echo '<<<<<< Success'
  }

  onFailure {
    echo '>>>>>> Failed'
  }

  enableSlackNotifications('#cmc-tech-notifications')
}
