#!groovy

@Library("Infrastructure@feature/archive-smoke-and-functional-yarn-tests")

String product = "cmc"
String component = "citizen-frontend"

withPipeline("nodejs", product, component) {
  after('build') {
    sh 'yarn setup'
  }

  enableSlackNotifications('@timj')
}
