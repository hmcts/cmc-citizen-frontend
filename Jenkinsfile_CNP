#!groovy

@Library("Infrastructure@smokey")

String product = "cmc"
String component = "citizen-frontend"

sh "docker-compose ps"

withPipeline("nodejs", product, component) {
  after('build') {
    sh 'yarn setup'
  }

  before('smoketest:aat') {
    env.IDAM_URL = 'http://betaDevBccidamAppLB.reform.hmcts.net'
    env.CITIZEN_APP_URL = env.TEST_URL
  }

  enableSlackNotifications('@timj')
}
